on: [push]

jobs:
  build:
    name: Push Sphinx Pages
    runs-on: ubuntu-latest
    steps:
    - uses: tdial2000/ILEX@dev
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        create_readme: true